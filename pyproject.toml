[project]
name = "persib-revenue-simulator"
version = "0.1.0"
description = "Streamlit revenue modeling tool for the Persib App membership product"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.2",
    "plotly>=6.7.0",
    "streamlit>=1.57.0",
]
